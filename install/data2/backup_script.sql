mysqldump -u root -p --no-create-info isystem3 account > "account.sql"
mysqldump -u root -p --no-create-info isystem3 user > "user.sql"
mysqldump -u root -p --no-create-info isystem3 measure > "measure.sql"
mysqldump -u root -p --no-create-info isystem3 asociacion > "asociacion.sql"
mysqldump -u root -p --no-create-info isystem3 discountdetail > "discountdetail.sql"
mysqldump -u root -p --no-create-info isystem3 discountreport > "discountreport.sql"
mysqldump -u root -p --no-create-info isystem3 module > "module.sql"
mysqldump -u root -p --no-create-info isystem3 product > "product.sql"
mysqldump -u root -p --no-create-info isystem3 productinv > "productinv.sql"
mysqldump -u root -p --no-create-info isystem3 productor > "productor.sql"
mysqldump -u root -p --no-create-info isystem3 productrequest > "productrequest.sql"
mysqldump -u root -p --no-create-info isystem3 proveedor > "proveedor.sql"
mysqldump -u root -p --no-create-info isystem3 reportes > "reportes.sql"
mysqldump -u root -p --no-create-info isystem3 requestrow > "requestrow.sql"
mysqldump -u root -p --no-create-info isystem3 requestrowproductor > "requestrowproductor.sql"
mysqldump -u root -p --no-create-info isystem3 transaction > "transaction.sql"
mysqldump -u root -p --no-create-info isystem3 transactiondetail > "transactiondetail.sql"
